//* Importaciones
const { isEmail } = require('./is.email');
const { isOnlyNumber } = require('./is.only.number');
const { isPassword } = require('./is.password');

module.exports = {
    isEmail,
    isOnlyNumber,
    isPassword,
};